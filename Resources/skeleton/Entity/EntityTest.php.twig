<?php

namespace {{ namespace }}\Tests\Entity;

use PHPUnit\Framework\TestCase;
use {{ namespace }}\{{ class }};

{% block class_definition %}
/**
 * Autogenerated test class for entity {{ class }}
 */
class {{ class }}Test extends TestCase
{% endblock class_definition %}
{
    {% for action in actions %}
        {%- include 'Entity/types/' ~ action.type ~ 'Type.php.twig' with {'classname': class, 'propname': action.name, 'namespace':namespace} %}
    {% endfor %}
}